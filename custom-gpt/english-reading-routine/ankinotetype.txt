<div class="card-frame {{Entry Type}}">
  <div class="front-content">
    {{Front}}
  </div>
</div>
---
<div class="card-frame {{Entry Type}}">
  <div class="front-content">
    {{Front}}
  </div>

  <div id="answer" class="answer-divider"></div>

  <div class="back-content">
    {{Back}}
  </div>

  {{#Notes}}
  <div class="user-notes">{{Notes}}</div>
  {{/Notes}}

</div>
---
.card {
  margin: 0;
  padding: 0;

  background: #f6f7f8;
  color: #1f2328;

  font-family:
    -apple-system,
    BlinkMacSystemFont,
    "Segoe UI",
    "Noto Sans SC",
    "Microsoft YaHei",
    Arial,
    sans-serif;

  text-align: left;
}

/* 限制内容宽度，避免在宽屏上横向铺满 */
.card-frame {
  box-sizing: border-box;
  width: 100%;
  max-width: 940px;
  margin: 0 auto;
  padding: 52px 42px 64px;
}

/* */
.front-content {
  text-align: left;
  overflow-wrap: anywhere;
}

/* Vocabulary / Expression 的词条 */
.vocabulary .front-content > div:first-child,
.expression .front-content > div:first-child,

/* Grammar 卡的 Front 当前是纯文本 */
.grammar .front-content {
  font-family:
    Georgia,
    "Times New Roman",
    serif;

  font-size: 56px;
  font-weight: 600;
  line-height: 1.18;
  letter-spacing: 0.01em;
}

/* 原始例句 */
.vocabulary .front-content > div:nth-child(2),
.expression .front-content > div:nth-child(2) {
  margin-top: 26px;

  font-size: 34px;
  font-weight: 500;
  line-height: 1.5;
}

/* Vocabulary 音标 */
.vocabulary .front-content > div:nth-child(3) {
  margin-top: 16px;

  color: #667085;
  font-size: 25px;
  line-height: 1.4;
}

/* 正反面分隔 */
.answer-divider {
  height: 1px;
  margin: 40px 0 32px;

  background: #cfd4dc;
}

/* 背面解释 */
.back-content {
  font-size: 31px;
  line-height: 1.72;
  text-align: left;
  overflow-wrap: anywhere;
}

/* Back 中各字段之间留出间距 */
.back-content > div + div {
  margin-top: 18px;
}

/* Anki 夜间模式 */
.card.nightMode {
  background: #242526;
  color: #f3f4f6;
}

.nightMode .vocabulary .front-content > div:nth-child(3) {
  color: #b8bec7;
}

.nightMode .answer-divider {
  background: #5c6168;
}

/* 手机和平板 */
.mobile .card-frame {
  padding: 30px 20px 44px;
}

.mobile .vocabulary .front-content > div:first-child,
.mobile .expression .front-content > div:first-child,
.mobile .grammar .front-content {
  font-size: 42px;
}

.mobile .vocabulary .front-content > div:nth-child(2),
.mobile .expression .front-content > div:nth-child(2) {
  margin-top: 22px;
  font-size: 28px;
}

.mobile .vocabulary .front-content > div:nth-child(3) {
  margin-top: 14px;
  font-size: 22px;
}

.mobile .answer-divider {
  margin: 32px 0 26px;
}

.mobile .back-content {
  font-size: 26px;
  line-height: 1.65;
}