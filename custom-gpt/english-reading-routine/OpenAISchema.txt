openapi: 3.1.0
info:
  title: Reading Memory Storage
  version: 0.1.0
servers:
  - url: 《》

paths:
  /exec:
    post:
      operationId: readingMemoryStorage
      summary: Store reading memory entries and rebuild the daily Anki file.

      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - entries
              properties:
                entries:
                  type: array
                  minItems: 1
                  description: Canonical reading memory entries.
                  items:
                    type: object
                    required:
                      - Entry Type
                      - Entry Content
                    properties:
                      Entry Type:
                        type: string
                        enum:
                          - vocabulary
                          - expression
                          - sentence
                          - paragraph

                      Entry Content:
                        type: string

                      Source Sentence:
                        type: string

                      Pronunciation:
                        type: string

                      Source Context:
                        type: string

                      Explanation:
                        type: string

                      Optional Translation:
                        type: string

                      Notes:
                        type: string

                      Value Fields:
                        type: array
                        items:
                          type: string

                      Grammar:
                        type: string

                      Custom Note:
                        type: string

                      Source:
                        type: string

                      Reference ID:
                        type: string

      responses:
        "200":
          description: Storage result.
          content:
            application/json:
              schema:
                type: object
                properties:
                  storedEntries:
                    type: integer

                  saved:
                    type: array
                    items:
                      type: object
                      properties:
                        filename:
                          type: string
                        url:
                          type: string

                  error:
                    type: string